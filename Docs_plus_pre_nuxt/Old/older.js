//old js functions

//build btns table



            /*

            //F btns
            //row 8 - 10
            //2 columns
            if(row > 7 && row < 11) {
                btnGroup = 'function';
                if(column > 1) {
                    column = 0;
                    row++;
                }

                if(column % 2 === 0) {
                    ledPosition = ledPosition + 3;
                } else {
                    ledPosition++;
                }

                if(column === 0 && row === 8) {
                    ledPosition = 64;
                }

            }

            //slider btns
            //row 11 - 14
            //2 columns
            if(row > 10 && row < 15) {
                btnGroup = 'slider';
                if(column > 1) {
                    column = 0;
                    row++;
                }

                if(row < 13) {
                    if(column % 2 === 0 ) {
                        ledPosition = ledPosition + 3;
                    } else {
                        ledPosition++;
                    }
                } else {
                    if(column % 2 === 0 ) {
                        ledPosition = ledPosition - 3;
                    } else {

                        if(row % 2 === 0) {
                            ledPosition--;
                        } else {
                            ledPosition++;
                        }
                    }
                }


                if(column === 0 && row === 11) {
                    ledPosition = 66;
                }

                if(column === 0 && row === 13) {
                    ledPosition = 79;
                }

            }

            //cross fader btns
            //row 15
            if(row > 14) {
                btnGroup = 'crossfader';
                if(column > 1) {
                    column = 0;
                    row++;
                }

                if(column === 0 && row === 15) {
                    ledPosition = 74;
                } else {
                    ledPosition = 75;
                }
            }

            //bpm
            //row 16
            if(row > 15) {
                btnGroup = 'bpm';
                if(column === 0 && row === 16) {
                    ledPosition = 80;
                } else {
                    ledPosition = 81;
                }
            }
            */